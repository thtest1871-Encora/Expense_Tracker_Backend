# Eureka Server

## 📌 Overview
The **Eureka Server** is the Service Registry. It acts as a phonebook where all other microservices register themselves so they can find each other.

## 🚀 Usage
- **Dashboard:** `http://localhost:8761`
- **Status:** Must be running **first** before any other service.

## ⚙️ Configuration
- **Port:** `8761`
